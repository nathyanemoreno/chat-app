export class UserMock {
  id: string;
  name: string;
  nickname: string;

  constructor(id: string, name: string, nickname: string) {
    this.id = id;
    this.name = name;
    this.nickname = nickname;
  }
}
